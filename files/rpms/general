bridge-utils
curl
dbus
euca2ools # only for testing client
gcc # dist:rhel6 [1]
git-core
openssh-server
openssl
openssl-devel # to rebuild pyOpenSSL if needed
libxml2-devel # dist:rhel6 [1]
libxslt-devel # dist:rhel6 [1]
psmisc
pylint
python-setuptools
python-unittest2
python-virtualenv
screen
tar
tcpdump
unzip
wget

# [1] : RHEL6 rpm versions of python-lxml is old, and has to be
# removed.  Several tools rely on it, so we install the dependencies
# pip needs to build it here (see tools/install_prereqs.sh)
